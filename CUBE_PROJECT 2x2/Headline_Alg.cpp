#include<iostream>
#include<vector>
#include<algorithm>
#include<map>
map<int,string> HEADLINE={{0,""},{1,"UUU"},{2,"UU"},{3,"U"}};
using namespace std;
int main()
{
    vector<int> pos;
    pos.push_back(pos[0]);
    for(int i=0;i<pos.size()-1;i++)
    if(pos[i+1]-pos[i]==1)
    return 
}